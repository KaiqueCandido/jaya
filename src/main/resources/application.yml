spring:
  # Database
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/mydb?createDatabaseIfNotExist=true
    username: postgres
    password: postgres
  # JPA properties
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    generate-ddl: true
# Server configuration
server:
  error:
    include-stacktrace: ON_PARAM
# Feign configuration
feign:
  client:
    exchange-rate:
      name: exchange-rate
      url: https://api.apilayer.com
      api-key: uoWFi3qvX2LlMKcXyE51npDU46In1AEo